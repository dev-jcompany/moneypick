# 00. PROJECT STATUS

> 이 문서는 프로젝트의 현재 상태를 나타낸다. 모든 AI는 작업 시작 전 이 문서를 가장 먼저 읽는다.

## 현재 Release

- **Release**: MVP (v0.1.0)
- **단계**: 초기 MVP 구현 완료, 운영 체계(AI-POS) 구축 중

## 진행률

| 영역 | 상태 | 비고 |
|---|---|---|
| 프론트엔드 페이지 (홈/카테고리/포스트/계산기/검색 등) | 완료 (Mock 데이터 기준) | `app/` 하위 라우트 구현됨 |
| 관리자(Admin) 페이지 | 완료 (Mock 데이터 기준) | `app/admin/**` |
| 데이터 계층 | Mock 데이터 (`src/data/*.ts`) | 실 DB/백엔드 연동 전 |
| API / 백엔드 연동 | 미착수 | `app/api` 없음 |
| 인증/권한 | 미착수 | 관리자 화면은 UI만 존재 |
| 배포 파이프라인 | 미확인 | Vercel 배포 가이드만 README에 존재 |
| 프로젝트 운영 체계 (AI-POS) | 진행 중 | 이 커밋에서 `docs/project` 구축 |

## 현재 작업

- `docs/project` 문서 체계(AI-POS) 최초 구축

## 다음 작업

- 실제 데이터 소스(DB 또는 CMS) 연동 방식 결정
- 관리자 화면의 인증/권한 처리 설계
- 배포 환경(Vercel 등) 확정 및 CI 구성

## 마지막 업데이트

- 2026-08-06
